
# Pharmasave Punch App (Final v11)

1) Create a Supabase project, run `supabase/migrations/final_phase_v11.sql`.
2) On Vercel, set env vars from .env.example.
3) Deploy. Cron routes register via `vercel.json`.

Push: VAPID keys required. Generate and set VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT.
